# Redis service for Railway
FROM redis:7-alpine

# No healthcheck needed - Redis image has built-in healthcheck
# Expose standard Redis port
EXPOSE 6379

# Default CMD is fine - redis-server runs on 6379
